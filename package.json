{
  "name": "anchor-tool",
  "version": "1.0.0",
  "scripts": {
    "build": "npx esbuild src/main.ts --bundle --outfile=dist/bundle.js --sourcemap",
    "watch": "npx esbuild src/main.ts --bundle --outfile=dist/bundle.js --sourcemap --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.4.0"
  }
}
